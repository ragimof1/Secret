<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Dashboard</title>
    <link rel="stylesheet" href="/css/dashboard.css">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-uWxY/CJNBR+1zjPWmfnSnVxwRheevXITnMqoEIeG1LJrdI0GlVs/9cVSyPYXdcSF" crossorigin="anonymous">
    <link rel="stylesheet" href="/css/main.css">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=IBM+Plex+Serif:wght@200&display=swap" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Cantarell&display=swap" rel="stylesheet">
</head>
<body>
    <h1 class="dashboard">Dashboard</h1>
    <div class="container mt-5">
        <div class="row">
            <div class="col-8 grid-secret">
                <% if(secrets.secrets){ %>
                    <% for(let i = 0; i < secrets.secrets.length; i++){ %>
                        <div class="card secret-card">
                            <div class="card-body">
                                <h5 class="card-title mt-2"><%= secrets.secrets[i].title %></h5>
                                <h6 class="card-subtitle mb-2 text-muted"><%= secrets.user %></h6>
                                <p class="card-text mt-4"><%= secrets.secrets[i].shortText %></p>
                            </div>
                        </div>
                    <% } %>
                <% }else{ %>
                    <h3>You don't have any secrets</h3>
                <% } %>
            </div>
            <div class="col-4 profile">
                <img src="/images/avtar.jpeg" alt="avatar" class="avatar">
                <h3 class="username mt-3"><%= username %></h3>
            </div>
        </div>
    </div>
</body>
</html>